{
  "name": "root-config",
  "version": "1.0.0",
  "description": "Single-Spa root config — the shell app that orchestrates all micro frontends. This is the entry point of the entire Single-Spa architecture. It does NOT render UI itself; it registers micro frontends and decides which one is active based on the current URL.",
  "scripts": {
    "start": "webpack serve --mode development --port 9000",
    "build": "webpack --mode production"
  },
  "dependencies": {
    "single-spa": "^5.9.5"
  },
  "devDependencies": {
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^4.15.1",
    "html-webpack-plugin": "^5.5.3"
  }
}
